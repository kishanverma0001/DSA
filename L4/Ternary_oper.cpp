#include<iostream>
using namespace std;

int main(){
    // int age;
    // cout<<"Enter age : ";
    // cin>>age;
    // bool isAdult = (age >= 18) ? true : false;
    // cout<<isAdult<<endl;

    int a = 5;
    int b = 4;
    int largest = (a >= b) ? a : b;
    cout<<"largest is : "<<largest<<endl;
    return 0;
}